import React from 'react';
import Navigation from '../components/Navigation';

const Resume = () => {
    return (
        <div>
            <header>
                <Navigation />
            </header>
        </div>
    );
};

export default Resume;